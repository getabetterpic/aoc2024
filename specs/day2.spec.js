import { describe, it } from "vitest";
import { safeIncrease, safeDecrease } from "../src/day2.js";

describe("day2", () => {
  describe("safeIncrease", () => {
    it("returns true if increase is less than or equal to 3");
  });
});
